const actionTypes = ['release', 'remove'];

const apiConfig = {
    tokenHeader: 'X-Token',
    siteTokenHeader: 'x-meli-token',
    urlPrefix: '/api/v1',
};

module.exports = {actionTypes, apiConfig};
